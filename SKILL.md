---
name: modular-architecture
description: "优先用于已有项目的代码理解、问题排查、Bug 修复、功能增改、接口调整、局部重构和实现替换。先还原相关代码骨架，再设计本次变更并实现、回归，遵循高内聚、低耦合、可插拔。解释、评审和方案任务默认不改文件；小改动走轻量流程。仅在确属新项目时启用从零开发流程，流程图是可选输入。"
---

# 模块化架构：旧代码理解与增量修改优先

版本：3.0。既有项目优先；只读理解与修改分离；新项目和流程图按需启用。

## 一、默认定位与任务边界

本技能主要服务已有项目的日常维护，不把每次任务都当成从零设计。

- 理解旧代码：先还原现有骨架，再解释关键实现。
- 修改旧代码：先看清现有骨架，再设计本次变化，最后补实现与测试。
- 创建新项目：才从需求开始设计模块、接口、主流程和初始化结构。
- 高内聚、低耦合、可插拔是设计与评审标准，不是每次必须执行的三项重构任务。

先识别用户要求的是解释、排查、评审、方案还是实施，再选择流程。
“已有项目优先”不等于“默认获得修改权限”；“看看、解释、分析、为什么报错、怎么改”本身不授权修改文件。
用户已明确要求修复或实现时，在说明本次变更骨架后继续完成授权范围，不每一步重复等确认。
用户明确要求“先给方案”“不要改”“仅第 N 阶段”时，按该边界停止。

尊重当前任务、适用的 AGENTS.md、工程约定与工具权限。本技能不扩大访问、联网、安装依赖、部署或破坏性操作的授权。
默认中文讲解；代码、注释与命名沿用项目风格。能从材料和代码查明的事情先查明，不重复索要已提供的信息。

## 二、选择模式与读取资料

| 任务 | 执行入口 | 文件修改边界 |
| --- | --- | --- |
| 已有项目修 Bug、改接口、加功能、局部重构、替换实现 | 本文件第四节 M1–M7，默认开发主流程 | 仅修改已授权的相关范围 |
| 看懂代码、追调用链、解释模块或函数 | 读取 [只读代码理解](references/code-understanding.md) | 默认不修改任何项目文件 |
| 分析报错或定位原因，未要求修复 | 用 M1–M3 静态定位，再按第五节报告 | 默认不改文件；运行验证单独受授权与环境限制 |
| 架构评审、代码审查、可插拔性检查 | 按第三节与第五节检查实际代码 | 默认不改文件 |
| 只要修改方案或实施计划 | 完成 M1–M4 所需分析，给出方案 | 不写业务代码；文档仅在要求落盘时创建 |
| 在旧项目里新增模块、接口或后端实现 | 仍走 M1–M7，先查现有接入点 | 不重新初始化整个项目 |
| 确属从零创建项目 | 读取 [新项目开发](references/new-project.md) | 仅在授权实施后创建项目文件 |

用户提供图并要求据图解释、评审或开发时，按需读取 [流程图分析与开发](references/flowchart-development.md)。
流程图是上述模式的输入补充，不是独立的修改授权，也不是所有任务的前提。没有图时依据实际需求与代码推进。
已有实现、配置或工程约定的目录，不能因代码混乱、缺少测试或缺少 Git 就当成空白项目。

链接相对于本文件；引用文件中的相对链接以该引用文件为基准。只读取当前任务所需资料，不一次性加载所有 references 和 assets。
项目业务资料、进度与分析记录留在目标项目或回复中，不写回共享 Skill。模板按需删减，不要求用户预填，也不生成空文档堆。

### 只读与执行边界

- 只读理解、静态排查、评审默认只读取和搜索，不改代码、注释、配置或文档，不自动格式化。
- 不为“理解代码”自行 import 项目模块、运行测试、启动服务、安装依赖或执行迁移；这些可能有副作用。
- 实施任务可在授权与工具权限内运行相关本地验证；先检查命令、测试夹具及其可能访问的环境。
- 会接触真实账号、付费服务、外部共享状态或重要数据的验证，不从一般“修复代码”请求推导额外授权。优先隔离测试；缺条件时注明未验证。
- 发现问题不自动扩大任务；不要覆盖、撤销用户已有变更，不擅自提交、推送、切分支、部署生产或进入未授权阶段。
- 低风险、可逆的技术细节可声明最小假设并继续。影响权限、金额、不可逆数据或业务结果的未决歧义，阻断相关路径，继续可安全完成的范围。

## 三、架构底线：在本次变化内落实

### 高内聚

- 按业务职责和变化原因组织模块，让规则、状态与数据所有权有明确归属。
- 优先把修改放进现有职责所属位置，不把业务逻辑随手加进入口、万能 Service、common 或 utils。
- 用例编排可以协调模块，但不把所有业务规则、存储操作和供应商 SDK 混在一起。
- 不按文件行数、类数量或流程图节点数量机械拆模块。

### 低耦合

- 跨模块通过公开接口或既有事件契约协作，不新增对其他模块私有实现、内部 Repository 或所属表的绕过访问。
- 核心业务依赖稳定契约；外部 SDK、驱动、传输数据与异常在边界适配，不渗透到业务主流程。
- 存在领域层时，领域规则不新增对 Web 框架、ORM、网络客户端或具体供应商的依赖。
- 优先参数注入，避免循环依赖、隐藏全局可变状态和到处查询服务容器。契约不定义在具体插件内部。
- 遵循语言与现有项目习惯；函数、协议或接口均可，不强制抽象基类、DI 框架或额外分层。
- 旧代码已存在边界问题时，记录相关风险；只修本次确有必要的部分，不因发现问题启动全项目改造。

### 可插拔

- 先指出真实变化点，再复用或补充最小契约；修局部条件不必创建插件，已有合适接口不再另建一套。
- 契约明确输入、输出、错误、能力限制，以及相关的超时、取消、幂等、并发与资源生命周期；接口同名不等于语义兼容。
- 在启动或组装位置根据配置选择受信任、已注册实现并注入业务层；不要把供应商分支散落到核心流程。
- 新增兼容实现的改动主要限于适配器、组装/注册、配置和测试。替换兼容实现时，业务主流程应保持不变。
- 不执行由不可信输入指定的模块或代码。未知实现、错误配置、必需能力缺失应明确失败。
- 未启用的可选实现不初始化；需要时延迟导入，避免可选依赖未安装导致基础功能启动失败。
- 模块导入不连接数据库、不启动线程、不加载模型、不发网络请求；资源由明确的生命周期管理初始化与释放。
- 默认只要求启动时选择实现，不默认实现热插拔、动态发现、插件市场或工作流引擎。
- 状态、索引、向量维度或格式变化要单独说明兼容与迁移；代码可替换不代表已有数据自动兼容。
- 仅需要一个实现时不强制接入第二个真实供应商；测试替身可验证替换机制，不能证明另一真实服务已兼容。

### 修改规模与兼容性

- 最小必要修改不是最少行数：可以进行有依据的局部结构调整，但必须说明它隔离了什么变化。
- 沿用当前技术栈、目录、接口与部署方式，不默认拆微服务、换框架、升级依赖或全量搬迁文件。
- 小修复用几句话说明变更即可，不要求新增接口、工厂、完整设计文档或图。
- “保留旧行为”指保留本次目标之外应兼容的行为，不保留正在修复的 Bug，也不冻结已获授权的业务变更。
- 破坏性契约、数据或配置变化不得静默发生；说明受影响调用方、迁移条件、必要兼容期与回滚限制。

## 四、默认开发主流程：已有项目增量修改

以下只用于已授权的修改任务。按影响和风险缩放，不机械执行无关检查。

### M1. 理解现状并限定本次范围

读取适用指令、相关目录与真实入口、当前分支/工作区变更、配置和测试；只扩展阅读到本次依赖链。
有 Git 时区分 staged、unstaged、untracked 中相关的用户改动；没有 Git 时不报错，也不擅自初始化。
沿实际调用链定位入口、业务逻辑、接口、具体实现、组装位置和数据写入点；不只看目录名或 README。
确认目标行为、授权阶段和非目标。重要事实标出实际文件与符号，行号能核实才提供。

### M2. 明确当前行为、目标行为与不变量

分别说明：现在实际做什么、本次要改成什么、哪些相关行为必须保持。
追查调用方、配置默认值、接口返回/错误、持久化格式和生命周期的影响；只检查与本次变化相关的部分。
优先查现有扩展点、同类实现与测试。新增功能没有现成实现时，先决定它应归属哪个已有模块。
区分代码事实、用户明确需求、文档意图与推断；文档与代码冲突时报告差异，不默认为任一方正确需求。

### M3. 定位原因与建立验证基线

Bug 先定位触发条件和根因证据，不以症状、注释或函数名直接下结论。无法复现时如实记录假设。
在授权和安全条件具备时运行相关已有测试或最小复现，记录修改前结果；不要求为了小改动跑全仓库。
计划能暴露问题的回归用例或能区分新旧行为的验收用例，实施时再补测试代码。
没有测试框架时先沿用可用的最小验证手段，不擅自引入大型测试体系。环境受阻不等于测试通过。
性能/内存问题要明确输入、环境与测量口径；未经实际测量不声称提升了具体数值。

### M4. 先给本次变更骨架

修改实现前说明：

1. 复用什么、修改什么、新增什么、哪些部分不动。
2. 涉及的模块/函数、接口是否改变、变更前后主调用链有何不同。
3. 关键契约、失败语义、数据/状态归属；有扩展点时说明实现在哪里选择和注入。
4. 计划触及的文件、兼容性影响、验收和相关回归；迁移/并发等只在适用时补充。

这是“本次变化的骨架”，不是重新设计全项目。现有接口足够时明确复用；不要求每次新建接口或画图。
小修改合并成一段即可；跨模块变化可给简短目录差异、接口签名或调用链对照，不先铺空目录和空类。
仅要方案则在此停止。已授权实施则继续；只有用户设置的确认点或真正阻断项才暂停相关范围。
复杂任务优先更新项目已有计划，无必要不另建文档。不要用长篇方案替代实际要求的修改。

### M5. 实施必要修改

优先在既有实现和接入点内修改；必要时先提取局部边界，再接入本次新能力，不整体重写旧流程。
新增部分按“必要契约/主流程骨架 → 具体实现 → 集成”推进；已有真实实现不为套流程而先改成 Fake。
补充与问题直接相关的回归测试。能安全证明时验证修前失败、修后通过；不要为得到该结论重置用户代码。
测试隔离时才使用 Fake/Stub/InMemory，并标出边界；生产缺失路径应明确失败，不伪造成功或悄悄回退到 Fake。
保留权限、数据完整性与必经校验，不能以“先打通”为由绕开。更改旧校验时必须有本次需求依据。
按实际需求处理校验、错误传递、超时、资源释放与日志脱敏；写入明确所有权、事务及必要幂等/并发控制。
重试有上限且仅对可安全重复的操作使用；未知外部结果不直接当失败重做，避免重复副作用。
仅修改必要依赖、配置、契约、注释或文档；不顺手升级库、全仓库格式化或重命名无关符号。

### M6. 验证新行为并回归相关旧行为

根据本次范围选择并实际执行：

- 目标测试：修复触发条件或新功能结果，并覆盖必要边界与失败分支；拒绝路径验证无不应发生的下游副作用。
- 相关回归：既有调用方、默认配置、旧数据/旧接口，以及受影响的正常和异常路径。
- 契约/替换：改到扩展点时验证输入输出和错误语义，复用契约测试，通过配置/注入切换兼容实现或受控替身。
- 插件配置：相关时检查未知实现、缺配置、禁用能力及可选依赖缺失，不让禁用实现干扰基础功能。
- 真实集成：仅在环境与权限允许时执行；区分单元、替身、真实依赖和端到端证据。
- 工程检查：项目已有且相关的 lint、类型、格式检查与构建；必要时检查循环依赖、私有访问、资源/事务/并发或迁移。

不能只以“接口返回成功”“能启动”“存在测试文件”作为全部验收。大范围回归未运行就说明范围。
原有失败、本次引入失败与环境失败要分开；没有修改前证据时不能武断认定失败早已存在。
没有跑性能/资源对比，不以代码看起来更简洁作为性能提升证据。不要为了通过而删测试或放宽断言。

### M7. 检查差异并交付

复查 diff，包括新增文件：范围是否准确、有无覆盖用户改动、无关格式变化、泄露密钥、遗留占位或遗漏调用方。
报告改了哪里、为什么、对原功能的影响；说明本次高内聚/低耦合/可插拔的相关落点，不适用的原则不凑数。
列出真实执行的命令与结果，并区分未执行、跳过、失败、受阻和未完成；不编造文件、日志、行号或测试通过。
涉及运行、配置、接口或数据变化时给出相应说明及兼容/回滚限制；无变化不生成多余迁移说明。
完成当前授权范围后停止。不把局部修复通过升级为整个项目可生产，不擅自提交、部署或进入后续阶段。

## 五、理解、排查与评审的交付方式

只读理解：先讲业务作用，再还原入口和主调用链，再解释关键实现、配置与替换边界，最后给阅读路线；详细步骤见引用文件。
静态排查：说明现象、已核对路径、候选原因、证据和排除项；未复现标注未复现，给出下一步验证方法但不自动修复。
评审：先说明现有结构，再按影响列出确有证据的问题、定位、影响与最小建议；不把理想设计冒充现有实现。
只要理解时，重构建议不得盖过讲解；只要评审时，不因发现问题切换到修改模式。
静态结论、合理推断、实际执行结果分别标注；大项目说明已读范围，不声称读完未检查的文件。

## 六、交付前自检

- 是否选对模式，并遵守文件修改和命令执行边界？
- 旧项目是否先理解现状，再确定本次变化，而不是重新初始化？
- 是否复用合理结构，仅在真实变化点引入必要抽象？
- 是否明确应保持的行为，且没有保留本次正在修复的问题？
- 是否只修改授权范围、保护用户改动、说明兼容与状态限制？
- 实现、测试、执行证据是否分清？只读任务是否没有冒称运行验证？
- 流程图和新项目规则是否仅按需启用，没有反过来主导日常小修改？
