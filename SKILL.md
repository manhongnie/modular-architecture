---
name: modular-architecture
description: "用于按流程图从零开发新项目，以及模块设计、功能开发、接口设计、代码重构、第三方集成和架构评审。将流程节点、分支与代码和测试对应，落实高内聚、低耦合、可插拔与先骨架后实现。新增或替换模型、存储、检索、支付等实现时使用。单纯文案、格式调整或不涉及模块边界的局部修复，不启动完整流程。"
---

# 模块化架构开发

版本：2.0。保留原架构约束，增加按流程图启动与演进项目的可选流程。

## 工作目标

在完成当前需求的前提下，使业务职责集中、模块依赖明确、实现能够替换。
遵循“先画骨架（模块、接口、主流程），再填肉（实现、集成、测试）”。

尊重当前任务要求、适用的 AGENTS.md、现有工程约定和工具权限。
本技能不授权额外的数据访问、联网、安装依赖、部署或破坏性操作。
说明默认使用中文；代码命名与注释风格遵循项目约定。

## 零、选择任务入口

先判定任务，不要求所有任务都走新项目初始化：

- **新项目，提供流程图或文字流程**：在设计或实施前，读取本技能的 [流程图驱动开发流程](references/flowchart-development.md)，先建立流程规范，再进入下文通用执行流程。
- **既有项目，按新图开发或修改流程**：同样读取该文件；先比较图、需求与代码的差异，再做增量开发，不重建整个项目。
- **新项目，没有流程图**：从现有需求提炼文字流程与最小项目约束；明确标为推导草案，不声称用户已提供或确认流程图。需要梳理分支、状态或验收映射时再读取上述文件。
- **一般模块开发或架构评审**：直接使用下文通用执行流程，不额外加载流程图资料。
- **仅解释或绘制流程图，不要求开发**：只完成用户要求的分析或文档，不启动代码实现。

引用路径均相对于本技能目录。按需读取具体文件，不把 references 和 assets 一次性全部载入。
流程图、项目特有业务规则和开发进度保存在目标项目中，不写回共享 Skill。
流程图描述业务行为，架构规则约束代码组织；两者结合使用，不把每个流程节点机械拆成模块、类或插件。
本技能提供工作指令，不新增图片查看、图形格式解析或联网权限；不能读取的材料必须明确说明。

## 一、架构约束

### 1. 高内聚：围绕业务职责组织代码

- 每个模块拥有明确的业务职责、公开入口和内部实现；相关规则与状态归属应集中。
- 按业务能力和变化原因划分模块，不机械地按文件长度、类数量拆分。
- 用例编排层可以协调多个模块，但不要把各模块的业务规则、数据库操作和外部 SDK 全塞进一个巨型 Service。
- 只把真实共享、语义稳定的能力提取为公共代码；不要把不相干的业务规则堆进 common、utils 或通用基类。

### 2. 低耦合：模块通过契约协作

- 跨模块只使用公开接口或项目已有的事件机制；不导入其他模块的私有实现，不直接访问其 Repository 或所属数据表。
- 核心业务依赖小而明确的契约，不依赖某个供应商、模型、数据库驱动或网络客户端的具体实现。
- 存在领域层时，领域规则不得依赖 Web 框架、ORM、HTTP 客户端或外部 SDK；在边界处转换外部数据结构与异常。
- 优先通过构造参数或函数参数传入依赖。避免循环依赖、隐藏的全局可变状态，以及业务代码到处查询全局服务容器。
- 契约放在调用方所属模块或独立、稳定的契约模块中，不把核心接口定义在具体插件包里。
- 遵循目标语言的接口习惯；可以使用协议、接口、函数或类型定义，不强制引入基类和依赖注入框架。

### 3. 可插拔：替换兼容实现不修改业务主流程

- 先指出当前需求中哪些能力确实需要替换，再设计扩展点；不要把每个类都做成插件。
- 为扩展点定义最小稳定契约：输入、输出、错误语义，以及相关的同步/异步方式、超时、取消、幂等性、生命周期和并发约束。
- 各实现必须满足相同的语义约定，而不只是方法名相同。能力不支持时应明确报告，不得伪造成功或悄悄改变语义。
- 在程序启动或组装位置集中选择具体实现，并将其注入业务代码。配置分支可以存在于组装位置，不应散落在业务流程中。
- 默认采用显式配置或注册表。只从受信任、预先注册的实现中选择，不执行由不可信输入指定的模块或代码。
- 新增兼容实现时，改动应主要局限于实现文件、注册或配置、测试；不应要求修改既有业务主流程。
- 未启用的可选插件不应被初始化；必要时延迟导入，避免未安装的可选依赖使整个程序启动失败。
- 必需插件缺失或配置错误时明确失败；可选能力关闭时提供约定好的禁用状态，不使用 Fake 冒充生产能力。
- 显式管理资源初始化与释放；模块导入时不要连接数据库、启动线程、加载大模型或发起网络请求。
- 默认只要求启动时选择或替换实现。除非需求明确，不实现热插拔、动态发现、插件市场或通用插件框架。
- 涉及持久化状态、索引、向量维度或数据格式时，单独说明迁移、重建与兼容条件；代码可替换不代表已有数据自动兼容。

### 4. 避免过度设计

- 优先沿用现有工程与部署方式，在既有结构内做最小必要调整；不要为了“解耦”自行拆成微服务。
- 没有独立业务职责或真实变化点时，不新增一层接口、工厂、服务或抽象基类。
- 同一个概念不要建立多套重复封装；新增抽象时说明它隔离了什么变化，以及增加了什么成本。
- 小功能允许使用少量文件、函数和参数注入；不要求机械套用固定目录、完整 DDD、事件总线或大型框架。
- 不顺手重构无关模块，不覆盖、撤销用户已有改动。遵守当前 Phase 的范围，不擅自进入后续 Phase。

## 二、执行流程：先骨架，再实现

### 第 1 步：读取现状，限定范围

先检查适用指令、工作区变更、相关目录、调用入口、接口、实现与测试；只阅读完成当前任务所需的内容。
新项目先确认目标目录是否已有文件、语言与运行环境约束；未初始化 Git 不视为错误，也不擅自覆盖目录或创建远程仓库。
流程图任务先按参考流程记录来源、业务节点、连线条件、状态、歧义和当前范围。

明确以下事项：

1. 本次目标、交付物和非目标。
2. 现有模块职责、关键依赖与需要保留的行为。
3. 当前扩展点、必须兼容的接口和数据。
4. 拟修改的文件、已有测试命令与可验证的验收条件。
5. 对流程图任务，哪些节点和分支属于本阶段，哪些缺口或冲突尚未解决。

区分已确认事实与假设。能从代码、配置或文档查明的内容先自行查明；只有无法查明、且会显著影响安全或架构的歧义才询问用户。

### 第 2 步：先给出骨架

在修改实现前，简洁说明：

- 模块划分：每个模块负责什么、不负责什么。
- 接口契约：关键类型或函数签名、输入输出及失败语义。
- 主流程：入口如何调用业务用例，用例如何使用接口，具体实现在哪里组装。
- 依赖边界：谁依赖谁，哪些跨模块访问禁止出现。
- 文件计划：新增或修改哪些文件，各自承担什么职责。
- 验收方案：如何验证功能、兼容性、边界与实现替换；涉及数据变更时补充事务、迁移和回滚说明。

采用与任务规模相称的表达：小改动用几句话，跨模块改动可附目录树、接口骨架或流程说明。
不要为了展示架构先批量创建空目录、空文件或无用抽象。

若用户只要求方案或评审，到此为止，不修改业务代码。
若用户已要求实施，可以在说明骨架后继续当前授权阶段，不必每一步重复等待确认；保留用户明确要求的确认点。
复杂任务优先更新项目已有实施计划；项目没有约定且确有必要时，可使用 docs/implementation-plan.md。
流程图任务补充“节点/分支 → 规则 → 模块或函数 → 测试”的映射，所有尚不存在的代码与测试只能标为计划。
新项目先给出最小可验收版本与分阶段计划；不把图中运行顺序机械当成代码开发顺序。
小项目可把流程、架构、映射和实施计划合并到一份文档，不强制产生多套文档。

### 第 3 步：用最小骨架打通流程

- 先定义必要的契约和数据结构，再搭建业务主流程与依赖组装入口。
- 流程图任务先实现本阶段最小可验证路径及必要的拒绝/失败分支；省略后续范围时必须显式禁用或返回明确的未实现错误，不得绕过必经校验。
- 需要隔离外部系统时，提供 Fake、Stub 或 InMemory 实现，用于开发验证和业务单元测试。
- 明确标识替身实现及其适用环境，不得将替身验证表述为真实服务联通或生产验收。
- 未实现的生产执行路径必须明确失败，不得用空实现、恒定成功或伪造数据掩盖缺口；抽象接口声明除外。

### 第 4 步：逐步补齐真实实现

- 按本阶段最小可验收功能逐步实现，不一次性扩展全部未来能力。
- 把供应商 SDK、数据库、模型推理和网络调用封装在各自适配器中，向业务层返回自有契约规定的数据与错误。
- 接入适配器时集中修改组装位置，避免让业务流程出现供应商专属分支。
- 按需求处理参数校验、超时、资源释放、日志及错误传递。日志不输出密钥或不必要的敏感数据。
- 重试必须有上限，并仅用于已确认可安全重复的操作；不要把未知结果直接当作失败重做。
- 涉及写入时明确数据所有权、事务边界和必要的幂等/并发控制；跨模块协作通过公开用例完成。
- 对既有项目先保留行为、再隔离变化；必要时使用临时兼容适配，并说明后续移除条件。
- 只有确有需要才增加依赖、修改配置或迁移数据，并遵守项目现有审批要求。
- 逐步更新流程映射与实施状态；图、文字规则和代码不一致时记录差异，不静默改图来迁就代码，不将未经确认的业务假设当成既定需求。

### 第 5 步：验证功能与架构

按本次改动选择并实际运行相关验证；不适用的项目注明原因。

1. 业务测试：使用替身隔离外部依赖，覆盖主要成功路径、失败路径与边界条件。
2. 契约测试：验证实现的输入输出、错误语义和能力约束；对多个兼容实现复用同一组契约测试。
3. 替换测试：保持业务主流程不变，通过注入或配置切换另一实现或受控替身，验证用例仍符合契约。
4. 集成测试：验证真实适配器与真实依赖的连接、行为及资源释放；环境不具备时明确未验证。
5. 插件配置测试：检查未知实现、缺失配置、禁用插件及未安装可选依赖等相关场景。
6. 边界检查：检查循环依赖、跨模块私有访问、核心层导入具体适配器等问题；优先使用项目已有静态检查或架构测试。
7. 工程回归：运行项目已有的格式检查、lint、类型检查及相关测试；涉及迁移或并发时增加相应验证。
8. 流程一致性：逐项核对本阶段的节点、条件和分支；覆盖成功、拒绝与异常，以及实际涉及的循环、并行和状态迁移，记录代码位置、测试用例与执行结果。不能只用节点数量或测试文件存在来宣称覆盖。

不要仅为证明可插拔而强行接入第二个真实供应商；受控替身可验证替换机制，但不能证明另一真实供应商已经兼容。
不要只凭目录名称判断架构合格，要检查实际导入、实例创建位置、数据访问和调用路径。

### 第 6 步：检查差异并交付

复查本次 diff，确认没有无关修改、未标明的占位实现、泄露的密钥或未处理的破坏性变更。

交付报告至少包含：

- 已完成范围，以及关键文件和接口的变化。
- 高内聚、低耦合、可插拔分别落实在哪里；没有适用扩展点时说明原因。
- 已执行的验证命令与结果，以及失败、跳过、未执行项目的原因。
- 如何运行或切换实现；涉及状态变更时说明兼容和回滚限制。
- 尚未完成的事项、风险与阻断点。
- 流程图任务额外列出：对应的图版本、已实现并已验证的节点/分支、已实现但未验证的部分、未实现和范围外部分、仍有效的假设以及图代码差异。

没有执行的测试不得写“通过”；仅使用替身时不得写“真实集成成功”。
没有满足当前验收条件时不得宣布全部完成；完成当前授权阶段后停止，不自行推进下一阶段或部署生产。

## 三、最终自检

交付前逐项检查，并在不满足时说明原因：

- 职责是否清楚，业务规则是否集中在所属模块？
- 模块是否通过公开契约协作，而非彼此读取内部实现或数据？
- 可替换能力是否依赖稳定接口，具体实现是否在组装位置注入？
- 替换兼容实现时，业务主流程是否保持不变，是否有验证证据？
- 状态、资源和失败语义是否明确，禁用插件是否不干扰基础功能？
- 是否遵守当前范围，避免无关重构、额外技术栈和推测性扩展？
- 测试结论是否有实际执行依据，未验证内容是否明确列出？

- 对流程图任务，本阶段的每个关键节点和分支是否有可追溯的规则、实现与测试，未识别或未验证项是否明确？
