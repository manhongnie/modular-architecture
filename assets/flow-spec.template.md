# 流程规范与实现映射

> 本文件是供 Codex 按需填写、删减的模板，不要求用户事先填完。
> 方括号内容为占位符，不代表已提供、已确认或已验证的信息。
> 小项目可合并章节；不适用项删除或注明原因，不生成空文档堆。
> 只读任务默认在回复中整理；旧项目优先记录现状和本次变化，不能把所有节点都列为待开发。

## 1. 目标、来源与范围

任务模式：[只读理解/排查/评审/方案/已有项目修改/新项目]
本次目标：[要解决的问题与可观察结果]
当前阶段：[本次授权范围]
非目标：[不做什么]
已知技术约束：[平台、语言、数据、外部服务、资源限制；未知项标明]

| 来源 ID | 源文件 / 文字说明 | 版本或定位 | 用途 | 读取/确认状态 |
| --- | --- | --- | --- | --- |
| S01 | [实际路径] | [页/子图/提交；未知则明示] | [行为依据] | [已读取/部分无法识别；是否用户已确认] |

整体流程复述：[用一小段文字说明入口、参与者、业务主线、成功与失败终点]

## 2. 节点、分支与状态

| 节点 ID | 来源定位 | 职责/参与者 | 输入与前置条件 | 输出与副作用 | 失败语义 |
| --- | --- | --- | --- | --- | --- |
| [沿用源图或添加的 ID] | [来源 ID + 位置] | [职责] | [约定或待定] | [约定或待定] | [约定或待定] |

| 分支/边 ID | 起点 | 条件或事件 | 终点 | 规则来源 | 是否本阶段 |
| --- | --- | --- | --- | --- | --- |
| [ID] | [节点 ID] | [准确条件；未知则记录] | [节点 ID] | [图/文字/假设] | [是/否及原因] |

[仅在相关时补充状态迁移、循环退出、并行汇合、事务边界和必须保持的业务不变量。]

## 3. 假设、冲突与阻断

| ID | 未知/冲突事项 | 当前处理建议 | 风险与受影响路径 | 状态与依据 |
| --- | --- | --- | --- | --- |
| A01 | [具体事项] | [低风险可逆假设，或阻断受影响实现] | [影响] | [未确认/已确认；实际依据] |

[分清图中明确内容与工程建议。不要自动把“建议”改成“用户已批准”。]

## 4. 现状、目标与本次变更骨架

[既有项目填写本次差异；只读任务填写实际结构与未确认项，不生成实施承诺；新项目标明无现有实现。]

| 流程/规则 | 当前行为与代码定位 | 目标行为及依据 | 本次处置 | 应保持行为/相关回归 |
| --- | --- | --- | --- | --- |
| [ID] | [已读到的实际 file::symbol；未确认则注明] | [明确需求或待确认] | [保留/修改/新增/停用/待定] | [不能意外改变的行为] |

[图中未画出的旧功能不自动停用；架构建议与现有实现分开。]

| 模块 | 负责 | 不负责 | 公开契约 | 依赖/扩展点 | 数据与资源归属 |
| --- | --- | --- | --- | --- | --- |
| [模块] | [职责] | [边界] | [关键接口] | [真实变化点] | [归属] |

关键输入输出与错误：[目标语言类型或清晰文字约定]
接口/函数签名：[复用哪些签名，本次改动哪些；新项目给最小骨架]
业务主流程：[旧项目给现状/变化对照，新项目给必要骨架；保留条件与失败语义]
组装位置：[根据配置创建具体实现并注入业务层的位置]
替换与兼容限制：[契约、状态或数据格式方面的限制]

## 5. 修改或阶段计划（按需，非每次必填）

| 阶段 | 覆盖节点与分支 | 交付物 | 验收条件 | 依赖/阻断 | 授权边界 |
| --- | --- | --- | --- | --- | --- |
| P1 | [明确范围] | [最小可验收实现] | [可验证结果] | [环境与业务缺口] | [仅本阶段/已授权后续] |

[内部阶段划分不自动要求每一步等待确认，也不扩大用户授权范围。]

## 6. 流程 → 代码 → 测试

| 流程 ID / 分支 | 规则与可观察结果 | 模块/接口 | 实现定位 | 测试定位 | 实现状态 | 验证状态/证据 |
| --- | --- | --- | --- | --- | --- | --- |
| [节点或分支 ID] | [必须满足的行为] | [契约] | [计划路径/实际 file::symbol] | [计划用例/实际定位] | [待实现/已实现/受阻/范围外] | [未执行/通过/失败/受阻/不适用；证据 ID] |

## 7. 验证记录与交付差异

[区分修改前/修改后、替身/真实依赖；只读静态分析不可填成已运行。缺少修改前证据时不能认定失败原先就存在。]

| 证据 ID | 执行命令 | 环境/依赖/替身条件 | 实际结果 | 对应测试或流程 | 日志或必要说明 |
| --- | --- | --- | --- | --- | --- |
| V01 | [真实执行命令] | [实际环境] | [实际结果；未执行就注明] | [ID] | [实际记录，不编造] |

已实现且已验证：[范围]
已实现但未验证：[范围与原因]
未实现/受阻：[事项与影响]
范围外：[事项与依据]
图与代码的差异：[未解决差异，或已核查范围内无差异]
仍有效的假设：[ID]
运行与切换实现：[真实命令/配置；未验证时注明]

## 8. 变更记录（按需）

| 变更 | 新来源/版本 | 受影响规则、代码、数据和测试 | 复验状态 |
| --- | --- | --- | --- |
| [变更内容] | [实际来源] | [影响范围] | [待复验/实际结果] |
